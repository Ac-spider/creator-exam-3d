# 2026-07-10 美术与地图优先 UI 视觉验收

## 自动截图矩阵

| 场景 | 结果 |
|---|---|
| 1280×720，六关逐关 | PASS：任务、造物输入、生成、放置和结束回合均在视口内；生成卡以受限浮层显示，不再把核心按钮挤出屏幕 |
| 1440×900，六关逐关 | PASS：棋盘保持主视觉，环境不遮挡单位、路径和敌方意图 |
| 1280×500，生成卡溢出 | PASS：卡片可滚动到放置按钮；进入放置模式后卡片让棋盘点击穿透 |
| 760×900，世界志抽屉 | PASS：抽屉全屏呈现，底部造物坞继续可用 |
| `prefers-reduced-motion: reduce` | PASS：核心状态完整，无依赖长转场的信息 |
| forced colors／高对比 | PASS：核心按钮、焦点与文本可辨认 |
| 阻断 `/assets/art/**` | PASS：渐变／程序化画面降级后，仍可生成、放置并结束回合 |

自动化还验证了断图场景中的真实回合推进：成功放置本地编译造物，并从第 1 回合进入第 2 回合。全部页面均未产生 `pageerror`。

## 人工视觉复核

- 六关环境可通过外圈水与屋顶、矿岩与灯轨、森林城墙与足印、双边旗与雾、圣树与记忆碎片、五关残片与裂隙环快速区分。
- 所有环境对象只承担呈现；没有发现覆盖可玩格、妨碍选格或压住单位／路径提示的对象。
- 地图优先层级成立：任务面板和造物坞清晰，棋盘仍是主要视觉区域；桌面生成卡浮在造物坞上方并可滚动，选择放置后不会拦截棋盘点击。
- 序章、长夜三裁切、空域桥接与结局 CG 的 HTML 文案安全区有效；固定图不包含生成文字、中心英雄或具体实时造物。
- `人 / 志 / 术` 同时只开一个；移动端世界志可滚动，底部造物坞保留在视口内。

截图保存在执行机器的临时目录 `creator-exam-art-ui-qa-final`，不作为不稳定的像素金图提交。
